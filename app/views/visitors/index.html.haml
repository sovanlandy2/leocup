%link{:href => "//blueimp.github.io/Gallery/css/blueimp-gallery.min.css", :rel => "stylesheet"}
/= render "vote_popup"
.container
	= render "voting"
- if I18n.locale == :km
	= render "slider"
- else
	= render "eng_slider"
.container
	
	= render "latest_result"
	= render "gallery"
	/= render "division"
